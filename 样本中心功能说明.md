# 样本中心功能说明

## 一、功能概述

样本中心模块是面向模型微调场景的样本数据管理平台，提供样本数据总览、样本集管理和样本详情查看三大核心功能，覆盖从样本采集、标注、质检到使用的全生命周期管理。

---

## 二、样本中心（总览页）

**页面路径**：`/sample`

样本中心总览页是整个样本管理体系的入口，以数据可视化方式呈现样本资源的全局概况，帮助用户快速掌握样本资产现状。

### 2.1 统计概览

页面顶部以统计卡片形式展示以下核心指标：

| 指标 | 说明 |
|------|------|
| 样本总量 | 当前系统中的样本总数（条） |
| 样本集数量 | 已创建的样本集总数（个） |
| 已标注样本 | 已完成标注的样本数量，同时展示标注率 |
| 高质量样本 | 质量等级为优质的样本数量，同时展示占比 |
| 样本覆盖领域 | 样本涉及的领域大类数量 |
| 平均样本质量 | 所有样本的平均质量评分及等级名称 |

数据来源：后端接口 `GET /api/sample/sample-statistic`

### 2.2 样本领域分布

以环形图展示样本在各业务领域中的分布情况，包括领域名称、占比和样本数量。支持动态加载后端数据。

### 2.3 样本质量分布

以环形图展示样本质量等级分布，质量等级分为：优质、良好、一般、较差。每个等级显示占比和样本数量。

### 2.4 样本类型分布

以横向柱状图展示不同样本类型（文本、图片、语音、视频等）的数量和占比分布。

### 2.5 样本增长趋势

以面积折线图展示近几个月的样本增长趋势，同时展示本月新增样本数和本月质量样本数及占比。

数据来源：后端接口 `GET /api/sample/sample-trend`

### 2.6 样本采集状态

以状态卡片网格展示当前样本采集流程各阶段的数量：

- 采集进行中
- 待标注
- 标注中
- 待质检
- 质检中
- 已完成

### 2.7 快捷导航

页面右上角提供"样本集管理"按钮，可一键跳转至样本集管理页面。

---

## 三、样本集管理

**页面路径**：`/sample-set`

样本集管理页提供样本集的创建、浏览、筛选、排序和下载等功能。

### 3.1 新建样本集

点击"新建样本集"按钮，弹出创建对话框，需填写以下信息：

| 字段 | 说明 | 是否必填 |
|------|------|----------|
| 样本集编号 | 样本集的唯一编码 | 是 |
| 样本集名称 | 样本集的显示名称 | 是 |
| 样本类型 | 从字典编码中选择（文本/图片/语音/视频等） | 是 |
| 样本领域 | 从字典编码中选择所属业务领域 | 否 |
| 业务系统 | 关联的业务系统名称 | 否 |
| 描述 | 样本集的详细说明 | 否 |

数据来源：样本类型和样本领域选项通过 `GET /api/sample/code-dict` 接口从字典编码表动态加载。

提交后调用 `POST /api/sample/save-sample-set` 保存。

### 3.2 样本集列表

以卡片视图展示所有样本集，每个卡片包含以下信息：

- 样本集名称
- 样本集编号
- 样本类型（图标标识：文本/图片/语音/视频）
- 质量等级（优质/良好/一般/较差，颜色区分）
- 样本规模（样本数量）
- 版本号
- 更新时间
- 热度值

### 3.3 多维度筛选

支持以下筛选条件组合过滤：

| 筛选项 | 说明 |
|--------|------|
| 名称 | 按样本集名称关键词搜索 |
| 样本类型 | 多选，按文本/图片/语音/视频等类型过滤 |
| 样本领域 | 多选，按业务领域过滤 |
| 质量等级 | 多选，按优质/良好/一般/较差过滤 |
| 样本规模 | 多选，按小型(<1万)/中型(1-10万)/大型(10-100万)/超大型(>100万)过滤 |
| 业务标签 | 按业务系统关键词搜索 |
| 更新时间 | 按起始日期过滤 |

提供"重置"按钮一键清空所有筛选条件。

### 3.4 排序

支持按以下字段排序，点击切换升序/降序：

- 更新时间（默认）
- 样本规模
- 质量等级
- 热度

### 3.5 样本集操作

- **查看详情**：点击卡片跳转至样本详情页，传递样本集编号和名称
- **下载样本集**：将样本集下所有样本文件打包为 ZIP 下载，图片类型样本会自动附带 YOLO 标注文件（.txt）和类别文件（classes.txt）

下载接口：`GET /api/sample/download-sample-set?setNo=xxx&fileName=xxx`

---

## 四、样本详情

**页面路径**：`/sample-detail?setNo=xxx&setName=xxx`

样本详情页展示指定样本集下的所有样本数据，支持预览、标注查看和质量评分。

### 4.1 样本列表

以表格形式展示样本集下的所有样本，主要字段包括：

| 字段 | 说明 |
|------|------|
| 样本名称 | 可点击预览（仅图片和音频类型） |
| 样本类型 | 文本/图片/语音/视频 |
| 后缀名 | 文件扩展名 |
| 标注状态 | 已标注/未标注 |
| 文件大小 | 文件体积 |
| 创建时间 | 样本创建时间 |
| 更新时间 | 样本更新时间 |
| 质量评分 | 星级评分组件（1-5星） |

数据来源：`GET /api/sample/get-samples?setNo=xxx`

### 4.2 筛选与分页

- **筛选条件**：支持按样本名称关键词搜索、按标注状态（已标注/未标注）过滤
- **分页**：每页展示 20 条，支持翻页浏览

### 4.3 文件预览

点击样本名称可打开预览弹框，根据文件类型提供不同的预览方式：

#### 4.3.1 图片预览

- 在 Canvas 上渲染图片
- 自动加载 YOLO 格式标注数据，绘制标注框和类别标签
- 标注框按类别着色，选中框高亮闪烁
- 支持点击标注框与右侧面板联动

标注数据来源：`GET /api/sample/get-annotations?filePath=xxx`

#### 4.3.2 音频预览

- 内置音频播放器，支持播放/暂停
- 进度条拖拽跳转
- 倍速播放（0.5x / 0.75x / 1x / 1.25x / 1.5x / 2x）
- 显示音频时长、当前播放时间
- 展示语音转写文字（ASR 结果）

转写文字来源：`GET /api/sample/get-audio-text?sampleNo=xxx&sampleName=xxx`

### 4.4 标注面板（图片预览时）

当图片存在标注数据时，预览弹框右侧展示标注面板：

- **标注列表**：展示所有标注框的类别名称，支持点击选中
- **类别筛选**：下拉选择特定类别，过滤标注列表
- **关键词搜索**：按类别名称关键词搜索标注项
- **双向联动**：
  - 点击面板中的标注项 → 左侧图片对应标注框高亮闪烁
  - 点击图片上的标注框 → 右侧面板自动滚动到对应行并高亮
- **类别统计**：展示各标注类别的数量汇总

### 4.5 质量评分

每个样本行提供星级评分组件（1-5星），对应质量等级：

| 星级 | 质量等级 | 编码 |
|------|----------|------|
| 5星 | 优质 | 01 |
| 4星 | 良好 | 02 |
| 3星 | 一般 | 03 |
| 2星/1星 | 较差 | 04 |

评分提交接口：`POST /api/sample/update-sample-score`

### 4.6 返回导航

页面标题左侧提供返回按钮，点击返回样本集管理页面。

---

## 五、页面流转关系

```
样本中心（/sample）
  └─ 点击"样本集管理" → 样本集管理（/sample-set）
       └─ 点击样本集卡片 → 样本详情（/sample-detail?setNo=xxx&setName=xxx）
            └─ 点击返回 → 样本集管理（/sample-set）
```

---

## 六、后端接口汇总

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/sample/code-dict` | GET | 获取字典编码（样本类型/质量等级/样本领域） |
| `/api/sample/sample-statistic` | GET | 获取样本中心统计数据 |
| `/api/sample/sample-trend` | GET | 获取样本增长趋势 |
| `/api/sample/query-sample-set` | GET | 查询所有样本集 |
| `/api/sample/save-sample-set` | POST | 新建样本集 |
| `/api/sample/get-samples` | GET | 查询样本集下的样本列表 |
| `/api/sample/serve-image` | GET | 读取并返回图片/音频文件 |
| `/api/sample/get-annotations` | GET | 获取图片 YOLO 标注信息 |
| `/api/sample/get-audio-text` | GET | 获取语音样本转写文字 |
| `/api/sample/update-sample-score` | POST | 更新样本质量评分 |
| `/api/sample/download-sample-set` | GET | 下载样本集（ZIP 打包） |
