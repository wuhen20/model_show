# ===== Web 框架 / 通用 =====
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
python-dotenv>=1.0.1
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiofiles>=23.2.1
APScheduler>=3.10.0

# ===== 国密加密（SM4_CBC）=====
gmssl>=3.2.1

# ===== LLM / 对话 =====
openai>=1.50.0

# ===== 小模型平台（dev-algomodel）=====
sqlalchemy>=2.0.0

# ===== 多模态目标检测（multimodal-llm）=====
Pillow>=10.0.0

# ===== 样本中心（sxy-sample-center，MySQL/Oracle/Hive）=====
pymysql>=1.1.0
oracledb>=1.4.0
pyhive>=0.7.0
thrift>=0.16.0
pure-sasl>=0.6.2
# thrift-sasl 声明依赖 sasl（需 C 编译，Windows 常装失败），改用 pure-sasl 作为纯 Python 替代。
# 安装时必须加 --no-deps 跳过 sasl 依赖：
#   pip install --no-deps thrift-sasl>=0.4.3
# thrift-sasl 运行时会先尝试 import sasl，失败后回退到 pure_sasl，因此两者共存即可正常工作。
thrift-sasl>=0.4.3

# ===== 知识管理（liuqi-knowledgebase）=====
httpx>=0.27.0
neo4j>=5.0.0          # Memgraph 驱动
minio>=7.0.0          # 对象存储（可选）
boto3>=1.34.0         # Ceph S3 兼容 API（图像采集使用）

# ===== 时序样本/预测/分析（time-series-large-model，集成自 data-analysis）=====
pandas>=2.2.2
openpyxl>=3.1.2
xlrd>=2.0.1
chronos-forecasting>=2.2.2
scikit-learn>=1.5.0
# PyTorch（GPU 加速）：直接 `pip install torch` 在 Windows 下默认安装 CPU 版，无法使用 GPU。
#   · RTX 50 系（Blackwell / sm_120）：需 cu128 及以上
#       pip install torch>=2.7.0 torchvision>=0.22.0 --index-url https://download.pytorch.org/whl/cu128
#   · 其它显卡请在 https://pytorch.org/get-started/locally/ 选择对应 CUDA 版本
torch>=2.7.0
torchvision>=0.22.0

# ===== 图像样本清洗 =====
cleanvision>=0.3.7
