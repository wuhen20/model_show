# 样本中心 & 样本集管理 —— 演示脚本

**总预估时长：8–10 分钟**

---

## 一、脚本概览

| 阶段 | 页面 | 讲解内容 | 预估时长 |
|------|------|----------|----------|
| 开场 | 样本中心 | 整体定位、页面概览 | 30 秒 |
| 区域 1 | 样本中心 | 顶部统计卡片（6 个指标） | 1 分钟 |
| 区域 2 | 样本中心 | 样本领域分布（饼图） | 1 分钟 |
| 区域 3 | 样本中心 | 样本质量分布（饼图） | 30 秒 |
| 区域 4 | 样本中心 | 样本增长趋势（折线图） | 30 秒 |
| 区域 5 | 样本中心 | 样本采集状态 | 45 秒 |
| 跳转 | — | 从样本中心跳转到样本集管理 | 15 秒 |
| 区域 6 | 样本集管理 | 页面概览 + 筛选栏 | 1 分 15 秒 |
| 区域 7 | 样本集管理 | 卡片视图 + 排序/视图切换 | 1 分钟 |
| 区域 8 | 样本详情 | 基本信息 + 样本明细列表 | 1 分钟 |
| 区域 9 | 样本详情 | 图片标注信息展示 | 1 分钟 |
| 区域 10 | 样本详情 | 语音播放 + 转写文本 | 45 秒 |
| 区域 11 | 样本详情 | 文本/视频样本展示 | 45 秒 |
| 区域 12 | 样本集管理 | 列表视图 | 30 秒 |
| 区域 13 | 样本集管理 | 新建样本集弹窗 | 1 分钟 |
| 收尾 | 样本集管理 | 总结亮点 | 30 秒 |

---

## 二、详细台词与操作配合

---

### 【开场】样本中心整体定位（30 秒）

> **台词：**
> "大家好，今天为大家演示的是样本管理体系的两大核心页面——**样本中心**和**样本集管理**。样本中心是一个全景驾驶舱，用于从宏观层面监控样本资源的整体情况；样本集管理则是一个精细化的管理后台，用于对每一个样本集进行增删改查和多维度筛选。两个页面各有侧重、配合使用。我们先从样本中心开始。"

> **操作：**
> - 页面已停留在「样本中心」
> - 鼠标缓慢扫过页面整体布局，不做点击

---

### 【区域 1】顶部统计卡片（1 分钟）

> **台词：**
> "首先看顶部的六个统计卡片，它们用一句话概括了整个样本体系的当前状态。从左到右依次是：**样本总量**、**样本集数量**、**已标注样本**、**高质量样本**、**样本覆盖领域**、**样本质量评分**。"
>
> "每个卡片包含了指标名称、核心数值、单位，部分卡片还带有环比变化信息——比如已标注样本会显示标注率，高质量样本会显示占比。"
>

> **操作：**
> - 鼠标从左到右依次指向每个卡片，每指向一个停留 3–4 秒
> - 重点在「已标注样本」和「高质量样本」两个卡片上稍作停留，用鼠标圈出变化提示文字（如"标注率 XX%"）

---

### 【区域 2】样本领域分布（1 分钟）

> **台词：**
> "接下来是三个可视化图表区域。左边这个饼图展示的是**样本领域分布**，可以看到目前样本覆盖了计量、采集运维、电网运维、设备管理、市场与交易等多个业务领域。"
>
> "饼图中心展示了样本总量数字，右侧的图例列表给出了每个领域的具体占比和样本量。这种设计让你一眼就能看出——哪些领域样本积累比较充足，哪些还需要补充。"
>
> "比如目前现场作业领域占比最高，达到了 XX%，其次是采集运维领域。这些数据可以帮助团队在做模型训练资源规划时，快速判断各领域的覆盖情况。"

> **操作：**
> - 鼠标指向「样本领域分布」图表，圈出标题
> - 将鼠标悬停在饼图的某一扇区上（echarts 交互效果会高亮），让观众看到扇区高亮
> - 鼠标移到右侧图例列表，从上到下缓慢划过

---

### 【区域 3】样本质量分布（30 秒）

> **台词：**
> "中间的饼图是**样本质量分布**，将全部样本按质量评分分为四个等级：优质（≥90 分）、良好（75–90 分）、一般（60–75 分）、较差（<60 分）。"
>
> "可以看到优质样本占比超过 70%，这是一个非常健康的分布。高质量的样本是模型效果的基础保障。"

> **操作：**
> - 鼠标指向「样本质量分布」标题
> - 鼠标指向图例中"优质"项的百分比数字，强调高占比
> - 快速扫过右侧图例

---

### 【区域 4】样本增长趋势（30 秒）

> **台词：**
> "右边折线图展示的是**样本增长趋势**，可以看到近期样本数量呈稳定增长态势。下方的摘要区域给出了本月新增样本数量和本月质量样本数量，还能看到质量样本占新增的比例。"
>
> "这说明我们的样本体系不仅在数量上持续增长，质量上也在同步跟进。"

> **操作：**
> - 鼠标指向折线图的上升趋势
> - 指向底部摘要中的"本月新增样本"和"本月质量样本"数字

---

### 【区域 5】样本采集状态（45 秒）

> **台词：**
> "再往下是**样本采集状态**区域，展示了当前全流程各环节的数据水位。六个状态卡片覆盖了从采集、待标注、标注中、待质检、质检中到已完成的完整流水线。"
>
> "每个卡片用不同颜色区分，带有脉冲光效。这样可以快速识别哪个环节有积压——比如如果'待标注'数量较高，就需要调配更多标注资源；如果'待质检'较多，则需要加强质检力量。"
>
> "这种流水线式的状态监控，对日常运营管理非常有价值。"

> **操作：**
> - 鼠标从「采集进行中」开始，从左到右依次指向 6 个状态卡片
> - 在「待标注」和「待质检」卡片稍作停留
> - 鼠标指向「已完成」卡片，强调已完成数量最大

---

### 【页面跳转】（15 秒）

> **台词：**
> "现在了解了样本中心的全景视图之后，我们来看如何管理具体的样本集。点击右上角的「样本集管理」按钮。"

> **操作：**
> - 鼠标移动到右上角「样本集管理」按钮并点击
> - 页面跳转到样本集管理页面

---

### 【区域 6】样本集管理 — 页面概览 + 筛选栏（1 分 15 秒）

> **台词：**
> "现在我们进入了**样本集管理**页面。和样本中心不同，这个页面聚焦于单个样本集的精细化管理。"
>
> "首先看顶部的筛选栏，这是这个页面最核心的交互区域。我们提供了七种筛选维度：**名称搜索**、**样本类型**（文本/图片/语音/视频）、**样本领域**、**质量等级**、**样本规模**、**业务标签**、**更新时间**。可以自由组合。"
>
> "右侧的'重置'按钮可以一键清空所有筛选条件。这种多维度、可组合的筛选设计，能帮助用户在海量样本集中快速定位目标。"

> **操作：**
> - 鼠标从左上到右依次指向每个筛选条件区域
> - 操作演示：在「样本类型」筛选框中，点开下拉，勾选"图片"，展示单选联动效果
> - 操作演示：点击「重置」按钮，展示一键清空效果
> - 鼠标指向筛选栏下方的结果计数「共 X 个样本集」

---

### 【区域 7】样本集管理 — 卡片视图（1 分钟）

> **台词：**
> "筛选栏下方是样本集展示区域。默认以**卡片视图**展示，每个卡片包含了样本集的完整关键信息。"
>
> "卡片顶部是**样本类型标签**（文本/图片/语音/视频）和**质量等级标签**（优质/良好/一般/较差），颜色做了语义区分——优质是蓝色、良好是绿色、一般是黄色、较差是红色。"
>
> "卡片主体显示**样本集名称**（点击可跳转详情）、**描述**和所属**领域/业务标签**。底部是四个元信息：样本规模、热度、版本号、更新时间。"
>
> "右下角还有一个三点菜单，支持**下载样本集**。"
>
> "工具栏右侧提供了四种**排序方式**：按更新时间、样本规模、质量等级、热度排序，点击可切换升序/降序。还有**卡片/列表视图切换**按钮，适应不同使用习惯。"

> **操作：**
> - 鼠标指向某个卡片，从上到下依次指向：类型标签→质量标签→名称→描述→领域标签→底部元信息
> - 鼠标点击名称，演示跳转到样本详情页（可选）
> - 鼠标点击右下角三点菜单，弹出"下载"选项
> - 鼠标点击排序按钮"热度"，展示排序切换效果
> - 鼠标点击「列表」视图切换按钮


### 【区域 8】样本详情页 — 基本信息 + 样本明细列表（1 分钟）

> **台词：**
> "点击样本集名称后，我们进入了**样本详情页**。这个页面分为三个核心区域：顶部面包屑导航、左侧样本集基本信息卡片、右侧样本明细列表。我们先看整体布局。"
>
> "左侧基本信息区展示了样本集的完整元信息：**样本集编号、名称、描述、所属领域、业务标签、样本类型、质量等级、样本规模、版本号**，以及**创建时间和最后更新时间**。相比卡片视图的摘要，这里提供了更全面的字段，方便用户完整了解该样本集的背景。"
>
> "右侧是**样本明细列表**区域，展示了该样本集下包含的所有样本条目。顶部有一个类型筛选标签栏，支持按**全部、文本、图片、语音、视频**进行快速筛选切换。每个样本条目以列表行形式展示，包含**样本编号、样本类型图标、文本摘要（文本类型）、缩略图（图片类型）、播放按钮（语音类型）**等关键信息。"
>
> "这种设计让用户可以在一屏之内，既了解样本集的宏观信息，又能快速浏览样本集中的具体样本条目。对于包含多种类型的样本集，类型筛选标签尤为重要。"

> **操作：**
> - 在区域 7 中点击某个样本集名称，页面跳转到样本详情页
> - 鼠标缓慢扫过页面整体布局（面包屑→左侧信息区→右侧明细区）
> - 鼠标从上到下依次指向左侧基本信息区的各个字段，在「质量等级」和「样本规模」处稍作停留
> - 鼠标指向右侧样本明细顶部的类型筛选标签栏
> - 操作演示：点击"图片"标签，列表筛选为仅显示图片类型样本
> - 鼠标指向某一条样本的缩略图，表示可点击查看


### 【区域 9】样本详情页 — 图片标注信息展示（1 分钟）

> **台词：**
> "在样本明细列表中，点击图片类型样本的缩略图，会打开**图片标注查看器**。这是样本详情页的核心功能之一。"
>
> "这个查看器采用分栏布局：**左侧为图片预览区**，展示原始图片并在图片上叠加标注框；**右侧为标注信息面板**，以列表形式展示所有标注对象。"
>
> "在左侧图片区，每个标注对象用一个**彩色矩形框**框出，框的颜色与标注类别对应——比如'电表'用蓝色框、'开关'用绿色框、'变压器'用橙色框。鼠标悬停在某个标注框上时，该框会高亮并弹出浮动标签，显示标注类别名称。"
>
> "右侧标注信息面板列出了图片中所有标注对象的详细信息：**标签名称、置信度（如适用）、标注框坐标（x/y/宽/高）、标注人、标注时间**。点击面板中的某一项，左侧图片上对应的标注框会高亮闪烁，实现双向联动。"
>
> "这种可视化标注审查方式，大大提升了标注质量审核的效率——审核人员可以直观地判断标注框是否准确、是否有遗漏或误标。"

> **操作：**
> - 在样本明细区点击一个图片类型样本的缩略图，弹出或跳转到图片标注查看器
> - 鼠标圈出左侧图片区上的几个标注框，强调彩色矩形框
> - 鼠标悬停在某个标注框上，展示高亮和浮动标签效果
> - 鼠标移到右侧标注信息面板，从上到下浏览标注列表
> - 在面板中点击某条标注记录，展示左侧图片对应标注框的联动高亮
> - 鼠标指向面板中的坐标信息和标注人字段


### 【区域 10】样本详情页 — 语音播放 + 转写文本（45 秒）

> **台词：**
> "接下来看**语音类型**样本的详情展示。在明细列表中切换到语音标签后，点击某个语音样本的播放按钮，进入语音播放详情。"
>
> "语音样本详情包含三个部分：**音频播放器**、**音频技术元信息**和**语音转写文本**。"
>
> "音频播放器提供了**播放/暂停、进度拖拽、音量调节、倍速播放**（0.5x/1x/1.5x/2x）等标准功能。下方显示音频的技术参数：**时长、采样率、声道数、文件格式、文件大小**。"
>
> "最下方是**语音转写文本**区域——如果该语音样本已经完成了转写标注，这里会显示对应的文本内容。转写文本支持随播放进度**同步高亮**，即播放到哪个时间点，对应的文字会自动高亮，方便审核人员比对语音内容与转写结果的准确性。"
>
> "这种语音+转写文本联动的设计，大大简化了语音样本的质量审核流程。"

> **操作：**
> - 在样本明细顶部点击"语音"类型筛选标签
> - 点击一个语音样本的播放按钮
> - 播放 3-5 秒后点击暂停，展示播放器基本操作
> - 鼠标指向进度条，强调可拖拽
> - 鼠标指向倍速选择按钮（0.5x/1x/1.5x/2x）
> - 鼠标移到技术参数区（时长、采样率等）
> - 鼠标指向转写文本区域，强调文字随播放同步高亮
> - 再次点击播放 2 秒，展示转写文本同步高亮效果


### 【区域 11】样本详情页 — 文本/视频样本展示（45 秒）

> **台词：**
> "最后快速看一下文本和视频类型样本的详情展示。"
>
> "**文本样本**详情直接展示文本全文内容，支持长文本滚动浏览。上方显示文本元信息：**字符数、语言类型、编码格式**。如果文本样本带有标注信息——比如实体标注、情感分类标注、意图识别标注等——标注内容会在文本中以**背景高亮或下划线**方式呈现，右侧或底部面板列出所有标注标签及其对应的文本片段，点击标签可快速定位到文本中的对应位置。"
>
> "**视频样本**详情内嵌视频播放器，支持**播放/暂停、进度拖拽、音量调节、全屏播放**。下方显示视频技术参数：**时长、分辨率、帧率、编码格式、文件大小**。如果视频有关键帧标注或时间段标注，会在播放进度条上以**标记点**展示，鼠标悬停可查看标注说明，点击标记点可跳转到对应帧。"

> **操作：**
> - 点击"文本"类型筛选标签，鼠标指向文本内容区域，向下滚动展示长文本
> - 鼠标指向文本元信息（字符数、语言类型等）
> - 鼠标圈出文本中的标注高亮内容，指向右侧标注标签面板
> - 点击"视频"类型筛选标签
> - 点击一个视频样本，播放 2-3 秒后暂停
> - 鼠标指向进度条上的标注标记点，悬停展示标注说明
> - 鼠标指向视频技术参数区域


### 【区域 12】样本集管理 — 列表视图（30 秒）

> **台词：**
> "切换到列表视图后，信息以表格形式展示，更加紧凑。每行列出了样本集名称、类型、规模、质量等级、版本号、热度、更新时间。右侧直接有下载操作按钮，方便批量浏览和对比。"

> **操作：**
> - 鼠标从上到下滑过列表表头各列
> - 停在某一行的质量等级标签和下载按钮上
> - 鼠标点击「卡片」切换回去


### 【区域 13】新建样本集弹窗（1 分钟）

> **台词：**
> "最后演示创建新样本集的功能。点击右上角的「新建样本集」按钮，弹出一个创建弹窗。"
>
> "表单包含：**样本集编号**（必填）、**样本集名称**（必填）、**描述**、**业务系统**、**样本类型**（必填，下拉选择）、**样本领域**（下拉选择）。"
>
> "设计上简洁实用——必填项有明确标记，下拉选项从数据字典接口动态加载，保证一致性和可维护性。填好后点击确定即可创建，取消则关闭弹窗。"

> **操作：**
> - 点击右上角「新建样本集」按钮，弹出弹窗
> - 鼠标依次指向每个表单项的 label
> - 在「样本集编号」输入框中输入示例值（如"SAMPLE-DEMO-001"）
> - 在「样本集名称」输入框中输入"演示样本集"
> - 点开「样本类型」下拉框，选择一个选项
> - 鼠标指向「确定」和「取消」按钮，然后点击「取消」关闭弹窗

---

### 【收尾】总结（30 秒）

> **台词：**
> "总结一下，今天我们演示了三个配合使用的页面：**样本中心**提供从统计卡片、可视化图表到采集状态的全景监控能力；**样本集管理**提供从多维筛选、卡片/列表双视图到新建样本集的精细化操作能力；**样本详情页**则深入到每条样本的内部，支持图片标注可视化审查、语音播放与转写文本联动、文本标注定位、视频关键帧标注等多种样本类型的详情展示与标注审核。三者配合，构成了一个从宏观到微观、从整体到个体的完整样本管理体系。谢谢大家。"

> **操作：**
> - 无特殊操作，自然结束

---

## 三、操作配合提示表

| 环节 | 何时操作 | 操作内容 | 注意事项 |
|------|----------|----------|----------|
| 开场 | 说出"全景驾驶舱"时 | 鼠标缓慢扫过页面全貌 | 速度要慢，让观众有时间看清布局 |
| 统计卡片 | 说出每个指标名称时 | 鼠标依次指向 6 个卡片 | 每个卡片停留 3–4 秒，不要过快跳过 |
| 领域饼图 | 说"可以看到"时 | 将鼠标悬停在饼图某一扇区 | 等待 echarts hover 动画触发后再移动 |
| 质量饼图 | 说"超过 70%"时 | 鼠标圈出图例中的百分比 | 动作幅度稍大，确保观众看清指向 |
| 趋势图 | 说"稳定增长"时 | 鼠标沿折线趋势方向滑动 | 从低点到高点方向 |
| 采集状态 | 说"完整流水线"时 | 从左到右依次指向 6 个卡片 | 在"待标注/待质检"处稍作停顿 |
| 页面跳转 | 说"点击按钮"时 | 点击「样本集管理」按钮 | 等页面完全加载后再继续讲解 |
| 筛选栏 | 说"样本类型"时 | 点开下拉框，勾选一个选项 | 演示完立即点「重置」恢复 |
| 卡片视图 | 说"每个卡片"时 | 鼠标指向一个具体卡片 | 从顶部标签到底部元信息，顺序讲解 |
| 点击名称跳转 | 说出"点击可跳转详情"时 | 点击某个样本集名称 | 等详情页完全加载后再继续讲解 |
| 样本详情-基本信息 | 说出"完整元信息"时 | 鼠标从上到下指向左侧各字段 | 在质量等级和样本规模处稍作停留 |
| 样本详情-明细列表 | 说出"类型筛选标签栏"时 | 点击"图片"类型筛选标签 | 观察列表刷新后再继续 |
| 图片标注 | 说出"标注查看器"时 | 点击图片缩略图，打开标注查看器 | 等待标注框渲染完成后再操作 |
| 图片标注-联动 | 说出"双向联动"时 | 在右侧面板点击一条标注，观察左侧联动高亮 | 确认联动效果明显 |
| 语音播放 | 说出"音频播放器"时 | 切换到语音标签，点击播放按钮 | 播放 3-5 秒暂停，注意控制音量 |
| 语音转写 | 说出"同步高亮"时 | 再次点击播放，观察转写文本高亮 | 确认同步效果可见 |
| 文本展示 | 说出"文本样本"时 | 切换到文本标签，滚动浏览内容 | 若有标注高亮，圈出标注部分 |
| 视频播放 | 说出"视频样本"时 | 切换到视频标签，点击播放 | 播放 2-3 秒暂停，指向标注标记点 |
| 排序切换 | 说"四种排序"时 | 点击一个排序按钮 | 观察列表刷新后再继续 |
| 视图切换 | 说"列表视图"时 | 点击「列表」按钮 | 切换完讲解 30 秒，再切回卡片 |
| 新建弹窗 | 说"点击新建"时 | 点击「新建样本集」 | 填写示例后点「取消」关闭，不实际创建 |
| 收尾 | — | 不做操作，自然结束 | 保持页面展示最终状态 |

---

## 四、演示前检查清单

- [ ] 后端服务正常运行，API 能返回真实数据
- [ ] 浏览器打开「样本中心」页面作为起始页
- [ ] 确认 3 个 echarts 图表正常渲染
- [ ] 确认统计卡片数值来自真实接口（非占位符"-"）
- [ ] 确认样本集管理页面的筛选下拉数据已加载（字典接口正常）
- [ ] 取消所有默认筛选条件，确保卡片视图有数据
- [ ] 确认「新建样本集」弹窗能正常弹出/关闭
- [ ] **确认样本详情页能正常跳转，基本信息和样本明细列表渲染正常**
- [ ] **确认包含图片样本的样本集，图片标注查看器能正常打开并显示标注框**
- [ ] **确认图片标注左侧预览区与右侧标注面板的双向联动效果正常**
- [ ] **确认语音样本的音频播放器可正常播放，转写文本同步高亮正常**
- [ ] **确认文本样本的标注高亮与标签面板联动正常**
- [ ] **确认视频样本播放器可正常播放，进度条标注标记点正常显示**
- [ ] **确认详情页的类型筛选标签栏切换正常（全部/文本/图片/语音/视频）**
- [ ] 关闭 IDE 侧边栏、工具栏等非演示内容，全屏展示浏览器
- [ ] 鼠标光标大小调整至适合演示（如有需要可系统设置中调大）